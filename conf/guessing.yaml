defaults:
    - base
    - _self_
finetune:
    seq_length: 4000
    gradient_accumulation_passes: 1024
    save_checkpoint_steps: 1000
llm:
    parameters:
        max_tokens: 500
test_llm:
    parameters:
        max_tokens: 500
vllm_config:
    vllm_kwargs:
        max_model_len: 4000  # Match seq_length for guessing task
actor:
    rollout_policy: pipelinerl.domains.guessing.generate_guessing_rollout
environment: null
dataset_loader: pipelinerl.domains.guessing.load_problems
train_dataset_names:
    - train
test_dataset_names:
    - test
world:
    actor_fraction: 1
    finetune_fraction: 1
eval_every_n_versions: 20000
model_path: Qwen/Qwen2.5-0.5B-Instruct